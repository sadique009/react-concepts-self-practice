export default function reducer(state, action) { // reducer fn accepts a state and an action.
    if (action === "increase") {
      return state + 1;
    } else if (action === "decrease") {
      return state - 1;
    }
    return null;
  }

//   Technologies : (Frontend) React, React Native

// Database : MySQL

// Language : Java, Javascript

// Development Tool : Git, Github

// Scripting language : Javascript

// Operating System : Windows

// Payment Gateway : Razorpay

// SDK : Expo and React Native 

// Other skills : 